# Runtime deps for the Vercel Python (API) deployment only.
# Deliberately slim: no streamlit / pandas / altair / uvicorn / langdetect —
# the API doesn't import them, and keeping them out stays well under Vercel's
# 250 MB function bundle limit. Local dev uses pyproject.toml / uv instead.
fastapi>=0.115
lyricsgenius>=3.0.1
sqlmodel>=0.0.22
sqlalchemy>=2.0
psycopg[binary]>=3.2
requests>=2.32
tenacity>=8.2
python-dotenv>=1.0
